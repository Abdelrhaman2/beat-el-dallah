# دليل استخدام لوحة الإدارة — بيت الدلة
# Admin Operator Guide — Beit El Dallah

دليل مبسط وغير تقني لإدارة متجر **بيت الدلة** اليومية، متابعة الطلبات، وتعديل الأسعار والعربون.
A non-technical operational manual for managing Beit El Dallah store, orders, and settings.

---

## 1. الدخول إلى لوحة التحكم (Accessing Admin Panel)
- **رابط اللوحة:** افتح الرابط التالي في المتصفح: `http://localhost:3000/admin` (أو رابط موقعك المباشر `/admin`).
- **رمز الدخول (Passcode):** `dallah2026admin` (قابل للتغيير من ملف الإعدادات).
- *ملاحظة هامة:* رابط لوحة الإدارة محمي ومنفصل تماماً ولا يظهر في روابط المتجر العام لحماية الخصوصية.

---

## 2. كيفية التعامل مع الطلبات الجديدة (Order Workflow)
1. **عند قيام عميل بطلب عبر المتجر:**
   - يسجل النظام الطلب فوراً برقم فريد (مثال: `BD-2026-00123`) بحالة **"قيد الانتظار" (Pending)**.
   - يفتح المتصفح محادثة واتساب مع متجرك وفيها رسالة منسقة بكافة التفاصيل والعربون المحسوب (25%).
2. **عند وصول رسالة العميل على واتساب:**
   - يرسل العميل صورة إيصال التحويل (فودافون كاش أو انستاباي).
   - توجه إلى لوحة الإدارة ← **"إدارة الطلبات"** (`/admin/orders`).
   - ابحث برقم الطلب أو برقم هاتف العميل.
   - تأكد من مطابقة مبلغ العربون المحول في الإيصال مع العربون المطلوب في جدول الطلب.
3. **تحديث حالة الطلب:**
   - من القائمة المنسدلة في عمود "حالة الطلب"، غيّر الحالة من **قيد الانتظار** إلى **تم التأكيد (Confirmed)**.
   - بعد تجهيز البن وتسليمه لشركة الشحن، غيّر الحالة إلى **جاري الشحن (Shipped)**.
   - عند تأكيد الاستلام من شركة الشحن، غيّر الحالة إلى **تم التوصيل (Delivered)**.

---

## 3. كيفية إضافة منتج جديد (Adding a Product)
1. توجه إلى صفحة **"إدارة المنتجات"** (`/admin/products`).
2. اضغط على زر **"إضافة منتج جديد"**.
3. املأ الخانات التالية:
   - **الاسم بالعربية:** (مثال: بن الدلة - الخلطة الذهبية).
   - **الاسم بالإنجليزية:** (مثال: Beit El Dallah - Golden Blend).
   - **القسم:** اختر من الأقسام الستة (بن الدله، ستار كوفي، اسبريسو، بن تركي، شاي، مستلزمات الكافيهات).
   - **السعر الأساسي:** السعر بالجنيه المصري.
   - **رابط الصورة:** ضع رابط صورة المنتج.
   - **حالة التوفر:** ضع علامة صح أمام "تفعيل المنتج" ليظهر للعملاء فوراً.
4. اضغط على **"حفظ التغييرات"**.

---

## 4. إيقاف منتج غير متوفر مؤقتاً (Disabling Out-of-Stock Items)
- لا يتطلب المتجر جرد مخزون معقد. إذا نفد أي صنف، اذهب إلى صفحة **"إدارة المنتجات"**.
- اضغط على زر الحالة بجانب المنتج ليتحول من الأخضر **"نشط بالمتجر"** إلى الرمادي **"معطل"**.
- يختفي المنتج فوراً من متجر العملاء ولا يمكن طلبه حتى تعيد تفعيله بضغطة زر واحدة.

---

## 5. تعديل نسبة العربون ورقم الواتساب (Editing Deposit & WhatsApp)
1. توجه إلى صفحة **"إعدادات المتجر"** (`/admin/settings`).
2. **نسبة العربون (%):** يمكنك تعديلها من 25% إلى أي نسبة أخرى (مثلاً 20% أو 30%) أو 0% إذا أردت إلغاء العربون مؤقتاً. يتم تطبيق الحساب فوراً على كل الطلبات الجديدة بالمتجر.
3. **رقم الواتساب:** اكتب رقم الهاتف الذي يستقبل رسائل الطلبات بصيغة كود مصر (مثال: `201012345678`).
4. **تعليمات التحويل:** يمكنك تحديث أرقام فودافون كاش أو حساب انستاباي أو الحسابات البنكية.
5. اضغط **"حفظ التغييرات"**.

---

## 6. إنشاء كوبونات الخصم (Creating Promo Coupons)
1. توجه إلى صفحة **"كوبونات الخصم"** (`/admin/coupons`).
2. اضغط على **"إنشاء كود خصم جديد"**.
3. اكتب الكود (مثلاً: `RAMADAN15` أو `WELCOME10`).
4. اختر نوع الخصم: **نسبة مئوية (%)** أو **مبلغ ثابت (ج.م)**.
5. حدد القيمة والحد الأقصى لمرات الاستخدام، ثم اضغط حفظ.
